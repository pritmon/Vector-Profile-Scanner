# This is an example of what variables should exist in your actual .env file.
# NEVER commit the real .env file with secrets inside to GitHub.

# Example:
# GCP_PROJECT_ID=my-google-cloud-project-id
# VERTEX_AI_MODEL_ENDPOINT=https://your-endpoint.googleapis.com
DATABASE_URL=sqlite:///data/local.db
ENVIRONMENT=development
